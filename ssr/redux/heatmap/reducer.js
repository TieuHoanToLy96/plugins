import produce from "immer"

const initState = {
}

const heatmapReducer = produce((draft, action) => {
  switch (action.type) {
    default: {
      break;
    }
  }
}, initState)

export default heatmapReducer